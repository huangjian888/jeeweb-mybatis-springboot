server:
  port: 8086 #提供者的端口
spring:
  application:
    name: service-gateway-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'

